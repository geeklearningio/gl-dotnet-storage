mode: ContinuousDeployment
branches: 
  dev(elop)?(ment)?$:
    tag: alpha
  features?[/-]:
    tag: alpha.{BranchName}
    mode: ContinuousDelivery
  (pull|pull\-requests|pr)[/-]:
    mode: ContinuousDelivery
  support[/-]:
    mode: ContinuousDelivery